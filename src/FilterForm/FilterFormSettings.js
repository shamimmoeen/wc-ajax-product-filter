import FilterFormTitle from './FilterFormTitle';
import FilterFormNav from './FilterFormNav';

const FilterFormSettings = () => {
	return (
		<div className={'editor-control'}>
			<FilterFormTitle />
			<FilterFormNav />
		</div>
	);
};

export default FilterFormSettings;

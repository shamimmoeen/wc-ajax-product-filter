import CustomTabPanel from '../CustomTabPanel';
import { __ } from '@wordpress/i18n';

const VisibilityRules = () => {
	return <h3 style={{ textTransform: 'uppercase' }}>visibility rules</h3>;
};

export default VisibilityRules;
